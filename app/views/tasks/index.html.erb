<h1>tasks</h1>
<%= link_to 'add new task', new_task_path %>

<ul style="list-style-type: none; padding: 0;">
  <% @tasks.each do |task| %>
    <li style="margin-bottom: 0.5rem;">
      <% if task.completed %>
        ✅
      <% else %>
        ⬜
      <% end %>
      <%= link_to task.title, task_path(task), style: "margin-left: 0.5rem;" %>

      <!-- Delete task form -->
      <form action="<%= delete_task_path(task) %>" method="post" style="display: inline;">
        <%= hidden_field_tag :_method, 'delete' %>
        <%= hidden_field_tag :authenticity_token, form_authenticity_token %>
        <button type="submit" style="background: none; border: none; cursor: pointer; margin-left: 0.5rem;">
          🗑
        </button>
      </form>
    </li>
  <% end %>
</ul>
