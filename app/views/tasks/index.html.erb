<h1>Tasks</h1>
<ul style="list-style-type: none; padding: 0;">
  <% @tasks.each do |task| %>
    <li>
      <% if task.completed %>
        ✅ <%= task.title %>
      <% else %>
        ⬜ <%= task.title %>
      <% end %>
    </li>
  <% end %>
</ul>
