<h1>task details</h1>

<% if params[:edit] %>
  <!-- render shared form for editing task -->
  <%= render 'form' %>
<% else %>
  <!-- display task details -->
  <div>
    <p><strong>title:</strong> <%= @task.title.downcase %></p>
    <p><strong>details:</strong> <%= @task.details.downcase %></p>
    <p><strong>status:</strong> <%= @task.completed ? '✅ completed' : '⬜ incomplete' %></p>
  </div>

  <!-- link to enable editing -->
  <%= link_to 'edit task', task_path(@task, edit: true) %>

  <!-- delete button -->
  <%= button_to task_path(@task), method: :delete, data: { confirm: "are you sure you want to delete #{ @task.title.downcase }?" }, class: 'delete-task' do %>
    🗑
  <% end %>
<% end %>

<!-- link to return to task list -->
<%= link_to 'back to tasks', tasks_path %>
